# VS Code IntelliSense Setup for CSS-first Styling

The theme no longer depends on a generated Tailwind token layer.

For now, the most useful editor support is:

- Liquid syntax support
- CSS autocomplete and validation
- Any future component class conventions you decide to standardize on, such as BEM

If Tailwind returns later, this file can be expanded again to cover the token-driven utility workflow.
